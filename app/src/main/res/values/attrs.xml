<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="AddSubController">
        <!--是否可以手动输入-->
        <attr name="editable" format="boolean"/>
        <!-- 左右2边+-按钮的宽度 -->
        <attr name="ImageWidth" format="dimension" />
        <!-- 中间EditText的宽度 -->
        <attr name="contentWidth" format="dimension" />
        <!--中间TextView的大小-->
        <attr name="contentTextSize" format="dimension"/>
        <!--中间字体的颜色-->
        <attr name="contentTextColor" format="color"/>
        <!--整个控件的background-->
        <attr name="all_background" format="color|reference"/>
        <!--左面控件的背景-->
        <attr name="leftBackground" format="color|reference"/>
        <!--右面控件的背景-->
        <attr name="rightBackground" format="color|reference"/>
        <!--中间控件的背景-->
        <attr name="contentBackground" format="color|reference"/>
        <!--左面控件的资源-->
        <attr name="leftResources" format="color|reference"/>
        <!--右面控件的资源-->
        <attr name="rightResources" format="color|reference"/>
    </declare-styleable>

    <declare-styleable name="DecimalTextView">
        <!-- 数字符号 默认¥ -->
        <attr name="public_decimal_symbol" format="string" />
        <!-- 数字符号的字体大小 默认与字体大小一致(-1) -->
        <attr name="public_decimal_symbol_size" format="dimension" />
        <!-- 是否显示数字符号 默认true -->
        <attr name="public_decimal_show_symbol" format="boolean" />
        <!-- 是否显示数字分号 默认false -->
        <attr name="public_decimal_show_commas" format="boolean" />
        <!-- 上限数字 默认1000000 -->
        <attr name="public_decimal_upper" format="float" />
        <!-- 小数点后位数 默认2位 -->
        <attr name="public_decimal_scale" format="integer" />
        <!-- 小数点后是否用0填充 默认false -->
        <attr name="public_decimal_fill_zero" format="boolean" />
    </declare-styleable>

    <declare-styleable name="DecimalEditTextView">
        <attr name="public_decimal_symbol" />
        <attr name="public_decimal_symbol_size" />
        <attr name="public_decimal_show_symbol" />
        <attr name="public_decimal_show_commas" />
        <attr name="public_decimal_upper" />
        <attr name="public_decimal_scale" />
        <attr name="public_decimal_fill_zero" />
    </declare-styleable>

</resources>